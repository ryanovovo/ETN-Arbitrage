{
    "configurations": [
        {
            "type": "debugpy",
            "request": "launch",
            "name": "Launch Main",
            "program": "${workspaceFolder}/${input:programPath}",
            "console": "integratedTerminal"
        }
    ],
    "inputs": [
        {
            "type": "pickString",
            "id": "programPath",
            "description": "Select the main program to launch",
            "options": [
                "main.py",
                "test/test_callback_update.py",
            ]
        }
    ]
}